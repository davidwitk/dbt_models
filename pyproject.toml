[project]
name = "dbt-models"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "dbt-core==1.11.8",
    "dbt-duckdb==1.10.1",
    "sqlfluff==4.0.4",
]

[tool.uv.workspace]
members = [
    "test",
]
