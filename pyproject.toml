[project]
name = "dbt-models"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "dbt-core==1.12.3",
    "dbt-duckdb==1.11.0",
    "sqlfluff==4.3.0",
]

[tool.uv.workspace]
members = [
    "test",
]
