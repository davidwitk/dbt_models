sources:
  - name: s3
    config:
      freshness:
        error_after: {count: 24, period: hour}
      loaded_at_field: _extracted_at
    tables:
      - name: raw_s3_file_inventory
        config:
          loaded_at_query: "select max(_extracted_at) from read_parquet('s3://data-lake-chodera/s3_data/file_inventory/*.parquet')"
          meta:
            external_location: "read_parquet('s3://data-lake-chodera/s3_data/file_inventory/*.parquet')"
