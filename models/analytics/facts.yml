models:
  - name: fct_imdb_movies_daily
    columns:
      - name: id
        data_tests:
          - not_null
          - unique
      - name: title
        data_tests:
          - not_null
          - unique:
              where: is_latest_day
  - name: fct_strasbourg_piscines_frequentation
    columns:
      - name: record_id
        data_tests:
          - not_null
          - unique
